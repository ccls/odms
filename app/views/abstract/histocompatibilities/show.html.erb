<%= edit_link() %>

<%= wrapped_yndk_spans(:abstract,:histo_report_found,
	:label_text => "Found?" ) %>
<%= wrapped_date_spans(:abstract,:histo_report_on,
	:label_text => "Date" ) %>
<%= wrapped_spans(:abstract,:histo_report_results,
	:label_text => "Results" ) %>

<%= abstract_pages(@abstract) %>
