<%= form_for(@abstract,:url => abstract_histocompatibility_path(@abstract)) do |f| %>

<%= f.error_messages %>

<fieldset><legend><%= @page_title %></legend>

<%= f.wrapped_yndk_select(:histo_report_found,
	:label_text => "Found?") %>
<%= f.wrapped_date_text_field(:histo_report_on,
	:label_text => "Date") %>
<%= f.wrapped_text_area(:histo_report_results,
	:class => 'autosize',
	:label_text => "Results") %>

</fieldset>

<%= f.submit_bar(controller.class.name) %>
<% end %>
