<h4><%= ( (@project.new_record?)?'Creating':'Editting' ) %> project</h4>

<%= form_for(@project) do |f| %>
<%= f.error_messages %>
<%= f.wrapped_text_field :key %>
<%= f.wrapped_text_field :description %>
<p><%= f.submit( (@project.new_record?)?'Create':'Update' ) %></p>
<% end %>
