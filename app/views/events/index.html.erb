<% sub_menu_for(@study_subject) %>
<% id_bar_for(@study_subject) %>

<h3>Events</h3>
<% unless @events.empty? %>
 <table id='events'><thead><tr>
  <th>Date<%#= sort_link(:occurred_on,'Date') %></th>
  <th>Project<%#= sort_link(:project,'Project') %></th>
  <th>Event Type<%#= sort_link(:type,'Event Type') %></th>
  <th>Description<%#= sort_link(:description,'Description') %></th>
  <th>&nbsp;</th>
 </tr></thead><tbody>
  <%= render :partial => 'event', :collection => @events %>
 </tbody></table>
<% else %>
 <p>This subject has no operational events.</p>
<% end %>
<% if current_user.may_edit_events? %>
<p><%= link_to 'add event', new_study_subject_event_path(@study_subject),
	:class => 'button' %></p>
<% end %>
