<%= form_for(@package) do |f| %>

<%# TODO NOT IN RAILS 3 %>
<%#= f.error_messages %>

  <%= render 'form', :f => f %>
  <p>
    <%= f.submit "Create", :id => nil %>&nbsp;
		<%= link_to( "Cancel", packages_path, :class => 'button' ) %>
  </p>
<% end %>
