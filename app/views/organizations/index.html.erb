<h4>Organizations:</h4>
<% unless @organizations.to_a.empty? %>
 <table id='organizations'><thead><tr>
  <th>Key</th>
  <th>Name</th>
  <th>Person</th>
  <th>&nbsp;</th>
 </tr></thead><tbody>
  <%= render :partial => 'organization', :collection => @organizations %>
 </tbody></table><!-- id='organizations' -->
<% else %>
 <p>Sorry, no organizations yet.</p>
<% end %>
<p><%= link_to( "Create new organization", new_organization_path,
	:class => 'button' ) %></p>
