<h4>Abstract:</h4>

<%= form_for(@abstract,:url => study_subject_abstract_path(@study_subject,@abstract)) do |f| %>
<%= f.error_messages %>
<p>Sorry, no general editting yet.</p>
<p><%= f.submit( 'Update' ) %></p>
<% end %>
