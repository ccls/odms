<% stylesheets('base/jquery.ui.all.css') %><%# for datepicker %>
<h3>New Waivered</h3>

<% form_for(@subject, :url => waivered_path,
	:html => { :class => 'edit_subject' }) do |f| %>
<%#= render 'form', :f => f %>

<%= f.error_messages %>

<%= render :partial => 'subjects/prelim_form', :locals => {:f=>f} %>
<%= render :partial => 'subjects/eligibility_form', :locals => {:f=>f} %>
<%= render :partial => 'subjects/consent_form', :locals => {:f=>f} %>
<%= render :partial => 'subjects/child_parent_form', :locals => {:f=>f} %>

<p><%= f.submit_link_to 'Create new waivered case subject' %></p>
<% end %>
