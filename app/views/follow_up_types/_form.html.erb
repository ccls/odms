<h4><%= ( (@follow_up_type.new_record?)?'Creating':'Editting' ) %> follow_up_type</h4>

<%= form_for(@follow_up_type, :html => { :class => 'edit_follow_up_type'} ) do |f| %>
<%= f.error_messages %>
<%= f.wrapped_text_field :key %>
<%= f.wrapped_text_field :description %>
<p>
<%= f.submit( (@follow_up_type.new_record?)? "Create" : "Update" )%>&nbsp;
<%= link_to 'Back', follow_up_types_path, :class => 'button' %>
</p>
<% end %>
