<h4>IneligibleReason:</h4>
<%= form_for(@ineligible_reason) do |f| %>



<%# TODO NOT IN RAILS 3, but I wrote custom method similar %>
<%= f.error_messages %>



 <%= f.wrapped_text_field :key %>
 <%= f.wrapped_text_field :description %>
 <p><%= f.submit( (@ineligible_reason.new_record?)?'Create':'Update' ) %></p>
<% end %>
