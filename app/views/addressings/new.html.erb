<% javascripts('new_addressing') %>

<% form_for(@addressing,
	:url => study_subject_addressings_path(@study_subject),
	:html => { :class => 'edit_addressing' }) do |f| %>
<%= render 'form', :f => f %>
<% end %>
