<h4><%= ( (@project_outcome.new_record?)?'Creating':'Editting' ) %> project_outcome</h4>

<%= form_for(@project_outcome, :html => { :class => 'edit_project_outcome'} ) do |f| %>
<%= f.error_messages %>
<%= f.wrapped_text_field :key %>
<%= f.wrapped_text_field :description %>
<%#= f.wrapped_collection_select :project_id, Project.all, 
  :id, :to_s, :include_blank => true,
  :label_text => "Project" %>
<p>
<%= f.submit( (@project_outcome.new_record?)? "Create" : "Update" )%>&nbsp;
<%= link_to 'Back', project_outcomes_path, :class => 'button' %>
</p>
<% end %>
