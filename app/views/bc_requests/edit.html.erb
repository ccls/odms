<% birth_certificates_sub_menu %>

<h4>Edit BC Request</h4>

<% form_for(@bc_request) do |f| %>
 <%= f.error_messages %>
 <%= f.wrapped_select :status, BcRequest.statuses %>
 <%= f.wrapped_text_area :notes %>


 <%= f.submit 'Update', :id => nil, :name => nil %>
 <%# forms with hidden submit buttons don't submit on 
		pressing enter in a text field in Chrome. %>
 <%#= f.submit_link_to( "Update" )%>


<% end %>
