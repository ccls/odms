<!DOCTYPE html PUBLIC "-//W3C//DTD XHTML 1.0 Transitional//EN"
       "http://www.w3.org/TR/xhtml1/DTD/xhtml1-transitional.dtd">
<html xmlns="http://www.w3.org/1999/xhtml" xml:lang="en" lang="en">
<%= render 'layouts/head' %>
<body>
<div id='container'>
 <%= render 'layouts/header' %>
 <%= subject_id_bar(@study_subject) if @study_subject.present? && @study_subject.persisted? %>
 <div class='table'>
 <div class='columns'>

<% if !content_for?(:side_menu) and @study_subject.present? and @study_subject.persisted? %>
<% subject_side_menu(@study_subject) %>
<% end %>
<% if content_for?(:side_menu) %>
<div id='left_side_menu'>
<%= yield :side_menu %>
</div><!-- id='left_side_menu' -->
<% end %>


  <div id='main'>
   <%#= yield :main   I don't remember what this was for, but I don't think its used%>

<% if @study_subject.present? && @study_subject.persisted?  %>
   <%= do_not_contact(@study_subject) %>
   <%= ineligible(@study_subject) %>
   <%= refused(@study_subject) %>
<% end	#if @study_subject.present? && @study_subject.persisted?  %>

   <div id='content'>
    <%= flasher() %>
    <%= yield %>
   </div><!-- content -->
   <%= yield :content_bottom %>
  </div><!-- main -->
 </div><!-- columns -->
 </div><!-- class='columns_table' -->
 <%= render 'layouts/footer' %>
</div><!-- container -->
</body>
</html>
