<div id='fullsearch'>
<%= form_tag(study_subjects_path,:method => :get) do %>
<%# dir and order should be recorded in the session so shouldn't be needed here %>
<%#= hidden_field_tag :order, params[:order], :id => nil %>
<%#= hidden_field_tag :dir, params[:dir], :id => nil %>
<%= text_field_tag :q, params[:q], :id => nil  %>
<%= submit_tag 'find' %>
<% end %>
</div><!-- subsearch -->
