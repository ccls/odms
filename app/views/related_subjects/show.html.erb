<% sub_menu_for(@study_subject) %>
<%# id_bar_for(@study_subject) %>

<h3>Related Subjects 
<% if @study_subject.is_case? %>
<span>
<% if @unrejected_controls.count > 0 %>
<%= link_to "add control ( #{@unrejected_controls.count} available )",
	new_case_control_path(@study_subject.id),:class => 'button' %>
<% else %>
No controls available.
<% end %>
</span>
<% end %>
</h3>

<%#= render :partial => 'family' %>
<%= render :partial => 'matching' %>
<%#= render :partial => 'controls' %>
<%= render :partial => 'rejected_controls' %>
