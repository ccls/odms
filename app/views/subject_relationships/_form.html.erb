<%= form_for(@subject_relationship, :html => { :class => 'edit_subject_relationship'} ) do |f| %>
<%= f.error_messages %>
<%= f.wrapped_text_field :key %>
<%= f.wrapped_text_field :description %>
<p>
<%= f.submit( (@subject_relationship.new_record?)? "Create" : "Update" )%>&nbsp;
<%= link_to 'Back', subject_relationships_path, :class => 'button' %>
</p>
<% end %>
