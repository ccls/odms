<%# @study_subject ||= @abstract.study_subject %>
<%# sub_menu_for(@study_subject) %>

<h4>Abstract:</h4>
<%= form_for(@abstract) do |f| %>
<%= f.error_messages %>
<p>Sorry, no general editting yet.</p>
<p><%= f.submit( (@abstract.new_record?)?'Create':'Update' ) %></p>
<% end %>
