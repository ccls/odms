<% sub_menu_for(@study_subject) %>
<% id_bar_for(@study_subject) %>

<h3>Subject Specimen Inventory</h3>
<% unless @study_subject.samples.empty? %>
<table class='sample_study_subject'><thead><tr>
<th>received on</th>
<th>sample id</th>
<th>project</th>
<th>type</th>
<th>subtype</th>
<th>status</th>
</tr>
</thead><tbody>
<% @study_subject.samples.each do |sample| %>
	<tr>
		<td><%= sample.received_by_ccls_at -%></td>
<td>&nbsp;</td>
<td>&nbsp;</td>
		<td><%= sample.sample_type_parent -%></td>
		<td><%= sample.sample_type -%></td>
<td>&nbsp;</td>
	</tr>
<% end %>
</tbody></table>
<% else %>
<p>Study Subject currently has no samples</p>
<% end %>
<p><%= ( current_user.may_edit_samples? ) ?
	link_to( "add new ...", new_study_subject_sample_path(@study_subject) ) :
	'&nbsp;'
%></p>

<hr/>

<p>Currently there is no field in samples for a "sample id" unless that is the "external_id".</p>
<p>Currently there is no field in samples for a "sample status".</p>
<p>Currently the "project" is not set. This should probably be pulled from the enrollment when that is set but is currently a habtm relationship.</p>


<h3>Homex sample tables</h3>
<table id='pending' class='sample_study_subject'><thead><tr>
<th>Pending Samples</th>
<th>type</th>
<th>kit sent</th>
<th>kit tracking no</th>
<th>sample tracking no</th>
</tr>
</thead><tbody>
<% @study_subject.samples.pending.each do |sample| %>
	<tr><td>&nbsp;</td>
		<td><%= link_to sample.sample_type.to_s, sample_path(sample) -%></td>
		<td><%=mdy sample.sent_to_subject_on -%></td>
		<td><%#= sample.kit_tracking_number %></td>
		<td><%#= sample.sample_tracking_number %></td>
	</tr>
<% end %>
<tr>
<td>&nbsp;</td>
<td><%= ( current_user.may_edit_samples? ) ?
	link_to( "add new ...", new_study_subject_sample_path(@study_subject) ) :
	'&nbsp;'
%></td>
</tr>
</tbody></table><!-- id='pending' -->

<table id='collected' class='sample_study_subject'><tbody><tr>
<th>Collected Samples</th>
<th>type</th>
<th>date collected</th>
<th>date received</th>
</tr>
<% @study_subject.samples.collected.each do |sample| %>
	<tr><td>&nbsp;</td>
		<td><%= link_to sample.sample_type.to_s, sample_path(sample) -%></td>
		<td><%= sample.collected_at -%></td>
		<td><%= sample.received_by_ccls_at %></td>
	</tr>
<% end %>

</tbody></table><!-- id='collected' -->
