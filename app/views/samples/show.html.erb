<% stylesheets(:sample) %>
<% sub_menu_for(@study_subject) %>
<% id_bar_for(@study_subject) %>

<div id='subject_control_bar'>
<h3>Subject Sample</h3>
<div class='buttons'>
<%= link_to( 'Edit', edit_sample_path(@sample),
	:class => 'button' ) %>
</div><!-- class='buttons' -->
</div>

<%= wrapped_spans :sample, :sample_type_parent,
	:label_text => 'sample type:' %>

<%= wrapped_spans :sample, :sample_type,
	:label_text => 'sample subtype:' %>

<%= wrapped_date_spans :sample, :sent_to_subject_on,
	:label_text => 'Sent to Subject on:' %>

<%= wrapped_date_spans :sample, :collected_on,
	:label_text => 'Collected on:' %>

<%= wrapped_date_spans :sample, :received_by_ccls_on,
	:label_text => 'Received by CCLS on:' %>

<%= wrapped_date_spans :sample, :sent_to_lab_on,
	:label_text => 'Sent to Lab on:' %>

<%= wrapped_date_spans :sample, :received_by_lab_on,
	:label_text => 'Received by Lab on:' %>

<%= wrapped_date_spans :sample, :aliquotted_on,
	:label_text => 'Aliquotted on:' %>

<%#= render 'shipping_info' %>
