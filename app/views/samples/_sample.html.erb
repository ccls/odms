<%
	classes = ['row']
%>
<%= content_tag_for( :tr, sample, :class => classes.join(' ') ) do %>
<td><%= mdyhm sample.received_by_ccls_at -%></td>
<td><%= link_to sample.sampleid, study_subject_sample_path(sample.study_subject,sample) -%></td>
<td><%= sample.project.key %></td>
<td><%= sample.sample_type_parent -%></td>
<td><%= sample.sample_type -%></td>
<td>&nbsp;</td>
<%# 



	I am inside the find form here, so I cannot add the
	delete/destroy form.  No nested forms in HTML.



%>
<% end %><%# content_tag_for %>

