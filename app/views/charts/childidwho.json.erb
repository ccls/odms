{
	"title":{
		"text":  "Childidwho Counts",
		"style": "{font-size: 18px; font-family: Verdana; text-align: center;}"
	},
	"x_legend":{
		"text": "Childidwho",
		"style": "{font-size: 12px;}"
	},
	"y_legend":{
		"text": "Count",
		"style": "{font-size: 12px;}"
	},
	"elements":[
		{
<%#	this works		"on-click": "http://cnn.com", -%>
<%# this works too			"on-click": "chart_click()", -%>
			"type":      "bar",
			"alpha":     0.5,
			"colour":    "#CC9933",
			"font-size": 10,
<%#=@study_subjects.collect(&:count).join(',')-%>
<%# could use this style to call something more than just alert -%>
<%# some values are so small that they may be difficult to click -%>
			"values" :   [<%=@study_subjects.collect{|i| 
	"{\"top\":#{i.count},\"bottom\":null,\"on-click\":\"alert('#{i.childidwho}:#{i.count}')\"}"
}.join(',').html_safe%>]
		}
	],
	"x_axis":{
		"stroke":1,
		"tick_height":10,
		"grid_colour":"#00ff00",
		"labels": {
			"labels": [<%=@study_subjects.collect{|i| "\"#{i.childidwho}\""}.join(',').html_safe%>]
		}
	},
	"y_axis":{
		"stroke":      1,
		"tick_length": 3,
		"grid_colour": "#00ff00",
		"offset":      0,
		"max":         <%=@study_subjects.collect{|i|i.count.to_i}.max%>
	}
}
